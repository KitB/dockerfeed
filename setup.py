#!/usr/bin/env python
# -*- coding: utf8 -*-
from setuptools import setup

setup(name='dockerfeed',
      version='0.1',
      packages=['dockerfeed'],
      zip_safe=True,
      scripts=['bin/dockerfeed'],
      )
